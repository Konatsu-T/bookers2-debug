<div class="container">
  <div class="row">
  	<h2>Follower Users</h2>
		<% if @users.count > 0 %>
		  <%= render '/users/index2', users: @users %>
		<% else %>
		  <p>ユーザーはいません</p>
		<% end %>
	</div>
</div>